# Database connection
RD_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/openbadges"

# Authentication
RD_JWT_SECRET="your-jwt-secret-here"
RD_JWT_EXPIRY_SECONDS=3600 # 1 hour

# GitHub OAuth
RD_GITHUB_CLIENT_ID="your-github-client-id"
RD_GITHUB_CLIENT_SECRET="your-github-client-secret"

# WebAuthn Configuration
RD_RP_NAME="Rollercoaster.dev"
RD_RP_ID="localhost" # Should match domain in production
RD_ORIGIN="http://localhost:5173" # Frontend origin

# Frontend URL for redirects
RD_FRONTEND_URL="http://localhost:5173"

# Email Configuration
RD_EMAIL_FROM="noreply@rollercoaster.dev"
RD_EMAIL_HOST="localhost" # SMTP server host
RD_EMAIL_PORT=1025 # SMTP server port (1025 for MailDev)
RD_EMAIL_SECURE=false # Use TLS
RD_EMAIL_USER="" # SMTP username (if required)
RD_EMAIL_PASS="" # SMTP password (if required)
RD_TOKEN_EXPIRY_HOURS=24 # Verification token expiry in hours
